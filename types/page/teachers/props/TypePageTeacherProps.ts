import { TypeLibPrograms, TypeLibTeacher } from '@/types/index'

type TypePageTeacherProps = {
  readonly programs: TypeLibPrograms | null
  readonly teacher: TypeLibTeacher | null
}

export default TypePageTeacherProps
